import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic smoke test - will be expanded with proper tests
    expect(true, isTrue);
  });
}
